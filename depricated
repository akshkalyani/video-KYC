// Route for user authentication and login access using JWT authentication token.
// app.post("/api-jwt", (req, res) => {
//   const userEmail = req.body.email;
//   const userPassword = req.body.password;

//   // Query the database to find the user with the provided email
//   db.get("SELECT * FROM contacts WHERE email = ?", [userEmail], (err, row) => {
//     if (err) {
//       return console.error(err.message);
//     }
//     if (!row) {
//       // User with the provided email does not exist
//       return res.status(401).send("Invalid email or password");
//     } else {
//       // Check if the password matches the one stored in the database
//       if (row.password !== userPassword) {
//         return res.status(401).send("Invalid email or password");
//       }

//       // If email and password are correct, create JWT token
//       const token = jwt.sign({ email: row.email }, secretKey);
//       // Set token as cookie
//       res.cookie("token", token, { httpOnly: true });
//       console.log({ token });
//       // Redirect user to a specific page
//       res.redirect("/cholaReg");
//     }
//   });
// });